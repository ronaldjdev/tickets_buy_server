// TODO(webhook): Pendiente de cablear.
// Webhook.di requiere las features config, contact, conversation, webhook y socket.
// Recrear desde celux (src/platform/di/Webhook.di.ts) cuando esas features se migren.
export {};
