// TODO(gateway): Pendiente de cablear.
// Gateway.di requiere las features config, credit, contact, message y payment
// (CreditRepository, configRepo, sendMessageUseCase, addPaymentUseCase).
// Recrear desde celux (src/platform/di/Gateway.di.ts) cuando esas features se migren.
export {};
