export interface CreateGatewayLinkDTO {
	creditId: string;
	amount: number;
	expiresInDays?: number;
}

export interface CreateWidgetSessionDTO {
	creditId: string;
	amount: number;
	expiresInDays?: number;
}
